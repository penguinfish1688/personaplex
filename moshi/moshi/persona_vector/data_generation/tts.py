# Create a TTS class that uses dia to convert prompts to speech audio files
# create class TTS and initialize dia model with pretrained weights
# only one method: synthesize(prompt: str, output_wav_path: str) keep things simple